# UI 方案：AI 对话助手

## 0. 文档信息

- 功能 ID：FEAT-004；所属 Sub：SUB-003；状态：草稿；类型：混合型；依据：SUB-003 `ui.md`。
- 文档版本：v3
- 变更摘要：v2 补充 §1.1–§1.3 布局/结构/状态流草图、§3.1–§3.4 上下文三态/气泡类型/截断/选区高亮草图、§4.1–§4.2 空状态与 busy 置灰草图、§5.1 `/both` 共存草图、§9 设计决策记录(5 项)。v3 移除窄屏 sheet 草图(改由集成方实现),§5 改述窄屏不在本 feat 范围,§6 验收同步移除窄屏要求,§9 决策记录同步移除该行。

## 1. 信息架构与入口

对话入口为可开合的侧边 `TapNoteChatPanel`。聊天面板由消息列表、上下文三态选择（选区/全文/无）、输入框、发送/中止按钮、工具调用结果气泡构成。

```text
创作者打开侧边 ChatPanel
  -> 选择上下文模式（选区/全文/无）
  -> 输入消息 -> 发送
  -> 流式渲染 text 与 tool part 状态
  -> 工具执行后气泡展示操作类型与结果
  -> 多轮继续
```

```mermaid
flowchart LR
  P[ChatPanel 侧边] --> CTX[上下文三态]
  CTX --> IN[输入框 发送/中止]
  IN --> ML[消息列表]
  ML --> TB[工具结果气泡]
  TB --> MUL[多轮继续]
```

### 1.1 桌面布局(可开合右侧抽屉)

抽屉收起态,编辑器占满主区域,右上角按钮可展开:

```text
┌────────────────────────────────────────────────────────────┐
│  TapNote  [✨ AI ▾]  [☾]                       [💬 对话 ▸] │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  BlockNote 编辑器                                         │
│                                                            │
│  - 第一点...                                              │
│  - 第二点...  [选中 →]                                    │
│  - 第三点...                                              │
│                                                            │
└────────────────────────────────────────────────────────────┘
        右上角 [💬 对话 ▸] 按钮打开抽屉;再次点击或 ✕ 关闭
```

抽屉展开态,编辑器宽度收缩,ChatPanel 固定宽 380–420px:

```text
┌────────────────────────────┬──────────────────────────────┐
│ TapNote [✨ AI ▾] [☾]      │ 对话助手              [✕]    │
├────────────────────────────┼──────────────────────────────┤
│                            │ 引用: [选区|全文|无]         │
│                            ├──────────────────────────────┤
│  BlockNote 编辑器          │ ▌用户:把第二点展开成段落   │
│                            │   § 选区 2 块                │
│  - 第一点...               │                              │
│  - 第二点... [高亮]        │ ◆ AI                        │
│  - 第三点...               │  正在思考...                │
│                            │  ◔ updateBlock 输入中 #b2  │
│                            │                              │
│                            ├──────────────────────────────┤
│                            │ [输入消息...    ] [发送]    │
└────────────────────────────┴──────────────────────────────┘
   编辑器宽度随抽屉开合自适应;抽屉关闭时焦点回到编辑器
```

### 1.2 ChatPanel 内部结构

```text
┌──────────────────────────────────┐
│ 对话助手                  [✕]   │  ← header(固定)
├──────────────────────────────────┤
│ 引用: [选区|全文|无]            │  ← 上下文 segmented(固定)
│   ⓘ 选区超 4K,请减少或改用全文  │     超限提示行(条件出现)
├──────────────────────────────────┤
│                                  │
│  ▌用户:把第二点展开成段落      │
│    § 选区 2 块                   │
│                                  │
│  ◆ AI                           │
│   已分析选区(2 块)。           │
│   ◔ updateBlock  输入中   #b2  │  ← 消息列表(可滚动)
│                                  │
│  ┌ ✓ updateBlock 已更新 #b2 ─┐ │  ← 工具结果独立气泡
│  └ [跳转到该块]               ┘ │
│                                  │
│  ┌ ⚠ updateBlock 版本冲突 #b3┐ │
│  └ [仅重试该操作]             ┘ │
│                                  │
├──────────────────────────────────┤
│ [输入消息...]                    │  ← 输入区(固定)
│            [Enter 发送]          │
│            Shift+Enter 换行      │
└──────────────────────────────────┘
```

- header 固定置顶,含面板标题与关闭按钮。
- 上下文 segmented 与超限提示行紧随 header,始终可见。
- 消息列表占据中部可滚动区域,用户消息右对齐、AI 消息左对齐。
- 输入区固定置底,显示当前模式(发送 / 中止 / busy 置灰)。

### 1.3 UI 状态流

```text
                          ┌─────────────────┐
                          │      idle       │  ← 默认,输入框可用
                          └────────┬────────┘
                                   │ busy.acquire("chat") 成功
                                   ▼
                          ┌─────────────────┐
              ┌─────────── │    sending      │  ← 请求中
              │            └────────┬────────┘
              │                     │ 收到首个 chunk
              │                     ▼
              │            ┌─────────────────┐
              │            │   streaming     │  ← 文本流式 + tool-call 输入中
              │            └────────┬────────┘
              │                     │ tool-call 完整,客户端 execute
              │                     ▼
              │            ┌─────────────────┐
              │  仅重试    │  tool-pending   │  ← 单 tool-call execute
              │ ┌──────────│  (单操作)       │
              │ │          └────────┬────────┘
              │ │                   │ execute 完成
              │ │                   ▼
              │ │          ┌─────────────────┐
              │ │          │   completed     │  ← 一轮结束,可继续多轮
              │ │          └────────┬────────┘
              │ │                   │ 用户继续输入
              │ │                   ▼
              │ │          ┌─────────────────┐
              │ └──────────│  sending(下一轮)│
              └───────────►│                 │
                          └─────────────────┘

  任一阶段冲突 / 前置失败 / 流中断:
              ┌─────────────────┐
              │     error      │  ← 显示可重试气泡
              │  revision 冲突 │     [仅重试该操作]
              │  / 前置失败    │     / [中止当前轮]
              │  / 流中断      │
              └─────────────────┘
  busy.release 在 completed / error / 中止 时触发
```

- 与 inline 共享 busy:进入 `sending` 前 `busy.acquire("chat")`,失败则输入框置灰(见 §4.2)。
- 「仅重试该操作」用最新 `documentRevision` 重新 execute 当前 `toolCallId`,不重发整轮、不破坏已成功操作(tech.md §6)。
- 「中止当前轮」停止流式,保留已成功 tool-call 结果,触发 `busy.release`。

## 2. 通用交互与视觉

- 上下文三态明确显示「引用选区 / 引用全文 / 未引用」（SUB-003 ui.md §2）。
- 截断提示：引用全文超预算时显示「文档已截断：共 N 块，此处含前 M 块」；>2× 改发大纲时说明。
- 工具状态：显示「工具输入中 / 成功 / 失败 / 冲突」状态（SUB-003 ui.md §2）。
- 操作类型以文字 + 图标表达（如「已插入块」「已更新块」「已删除块」），不能仅以颜色区分。
- busy 时另一入口（inline slash/按钮）禁用并文字说明；chat 进行中时自身输入框可中止。
- 不因一个编辑器会话阻止其他会话（SUB-003 ui.md §2）。
- 复用 `@workspace/ui` 控件。

## 3. 字段、操作、校验与反馈

- 上下文选择：单选三态（选区/全文/无）。
- 输入框：消息文本；`Enter` 发送（Shift+Enter 换行）；空则发送禁用。
- 发送：`busy.acquire("chat")` 成功后发送；失败则输入框置灰。
- 中止：流式中显示中止按钮，停止当前轮。
- 工具气泡：展示工具名、目标块、输入中/成功/失败/冲突状态；冲突提供「重试」。
- 校验：选区超 4K 拦截提示；工具输入 Zod 校验失败展示错误。

### 3.1 上下文三态 segmented control

默认 `无`,切换到 `选区`/`全文` 时编辑器侧保留高亮直到发送(见 §3.4),消息气泡携带引用 chip:

```text
默认(无):
┌──────────────────────────┐
│ 引用: [ 选区 | 全文 |•无] │   • = 当前选中
└──────────────────────────┘   不发 documentState,不暴露 getDocumentSnapshot

选区:
┌──────────────────────────┐
│ 引用: [•选区 | 全文 | 无] │   选区 2 块 / 约 0.8K tokens ✓
└──────────────────────────┘
   ⓘ 选区超 4K,请减少或改用「引用全文+指令」     ← 超限时拦截提示

全文(预算内):
┌──────────────────────────┐
│ 引用: [ 选区 |•全文 | 无] │   全文 12 块 / 约 3.2K tokens ✓
└──────────────────────────┘

全文(超预算,截断):
┌──────────────────────────┐
│ 引用: [ 选区 |•全文 | 无] │   全文 47 块 / 约 11K tokens
└──────────────────────────┘
   ⓘ 文档已截断:共 47 块,此处含前 18 块

全文(>2× 预算,改发大纲):
┌──────────────────────────┐
│ 引用: [ 选区 |•全文 | 无] │   全文 120 块 / 约 38K tokens
└──────────────────────────┘
   ⓘ 文档过长,已发送结构化大纲
```

### 3.2 消息气泡类型

用户消息(右对齐)携带上下文引用 chip:

```text
                       ▌用户:把第二点展开成段落
                         § 选区 2 块              ← 引用 chip
```

AI 文本流式(左对齐),光标 `◌` 表示流式进行中:

```text
▌AI
│ ◌ 正在思考...
└
```

工具调用状态(嵌入 AI 消息内,与 AI 文本同属一条 UIMessage.parts):

```text
▌AI
│ 已分析选区。
│ ◔ updateBlock  输入中...   #b2   ← 旋转/输入中
│ ◑ updateBlock  输入中...   #b3
│ ◐ updateBlock  输入中...   #b4
└
```

工具结果独立气泡(在 AI 消息下方独立展示成功 / 冲突 / 前置失败):

```text
成功:
┌ ✓ updateBlock  已更新块    #b2 ─┐
│  目标:第二点                  │
│  [跳转到该块]                  │
└────────────────────────────────┘

版本冲突(可重试):
┌ ⚠ updateBlock  版本冲突     #b3 ─┐
│  文档已变更,该块当前 revision=8 │
│  期望 revision=5                 │
│  [仅重试该操作]                  │  ← 用最新 revision 重试当前 toolCallId
└──────────────────────────────────┘

前置条件失败(可重试):
┌ ⚠ updateBlock  目标块不存在 #b4 ─┐
│  块 #b4 已被删除                 │
│  [仅重试该操作]                  │
└──────────────────────────────────┘
```

错误气泡(流中断 / 认证失败,不暴露服务端堆栈):

```text
▌AI ✗
│ 流式中断,可重试或继续多轮。
└

▌AI ✗
│ 认证已过期,请重新登录。
└
```

### 3.3 截断与按需读取提示气泡

截断 / 大纲提示在 AI 首条消息内说明,确保用户知道发出去的 documentState 已被处理:

```text
截断:
▌AI
│ [文档已截断:共 47 块,此处含前 18 块]
│ 我先基于前 18 块处理,如需更多请允许「按需读取」。
└

大纲:
▌AI
│ [文档过长,已发送结构化大纲]
│ 收到 120 块的大纲,基于标题与首段摘要处理。
└
```

按需读取(`getDocumentSnapshot`,仅「引用全文」+ 允许按需读取时暴露):

```text
▌AI
│ 我需要更多内容来准确改写。
│ ⊙ getDocumentSnapshot  读取中  from=b19, max=10 块
└
   ┌ ✓ getDocumentSnapshot 已返回 ─┐
   │  返回块 b19-b28,约 1.8K tokens │
   └────────────────────────────────┘
```

### 3.4 引用选区的编辑器侧视觉

「引用选区」模式下,编辑器侧保留选区高亮直到发送完成,消息气泡同步显示 chip:

```text
┌────────────────────────────┬──────────────────────────────┐
│  BlockNote 编辑器          │ ▌用户:把这段翻译成英文      │
│                            │   § 选区 2 块                │
│  - 第一点...               │                              │
│  ┌──────────────────────┐ │ ◆ AI                        │
│  │ 第二点 [高亮保留]     │ │  ◔ updateBlock 输入中 #b2 │
│  └──────────────────────┘ │                              │
│  - 第三点...               │                              │
└────────────────────────────┴──────────────────────────────┘
   发送后编辑器侧高亮清除,消息气泡保留 chip 作为审计回执
```

## 4. 加载、空状态、错误状态与权限状态

- 加载：流式首块出现前显示思考指示。
- 空状态：无消息时输入框为主，提示可用上下文模式。
- 错误：流失败/工具失败展示可行动提示；认证失败提示重新认证；不暴露服务端堆栈。
- 权限/busty：另一 AI busy 时输入框置灰并说明原因。
- 冲突：revision/前置条件冲突气泡展示「版本冲突，可重试」。

### 4.1 空状态

```text
┌──────────────────────────────────┐
│ 引用: [ 选区 | 全文 |•无]       │
├──────────────────────────────────┤
│                                  │
│        💬 开始对话               │
│                                  │
│  · 选「引用选区」改写选中内容    │
│  · 选「引用全文」处理整篇文档    │
│  · 不引用时,可与 AI 自由对话   │
│                                  │
├──────────────────────────────────┤
│ [输入消息...]                    │
│            [发送]                 │
└──────────────────────────────────┘
```

### 4.2 busy 置灰态

内联 AI 进行中时 ChatPanel 输入框置灰并显示原因:

```text
┌──────────────────────────────────┐
│ 引用: [ 选区 | 全文 |•无]       │
├──────────────────────────────────┤
│  ▌用户:把第二点展开成段落      │
│                                  │
│  ◆ AI                           │
│   正在思考...                   │
├──────────────────────────────────┤
│ [输入框置灰]                     │
│ ⏸ 内联 AI 进行中,完成后可用   │  ← busy 提示行
└──────────────────────────────────┘
```

chat 自身进行中,可中止当前轮:

```text
┌──────────────────────────────────┐
│ ▌用户:把第二点展开成段落      │
│                                  │
│ ◆ AI                            │
│  正在思考...                    │
│  ◔ updateBlock 输入中 #b2      │
├──────────────────────────────────┤
│ [输入框置灰]                     │
│        [中止当前轮]              │  ← 中止按钮
└──────────────────────────────────┘
```

## 5. 国际化、可访问性与响应式

- 默认 zh-CN，操作结果/错误/截断提示可由字典替换（SUB-003 ui.md §3）。
- 聊天面板支持焦点陷阱、`Escape` 关闭、发送后焦点恢复、`aria-live` 流状态播报。
- 不引用状态不误导用户为已发送文档（SUB-003 ui.md §4）。
- 窄屏(<768px)的具体呈现方式(底部 sheet / 抽屉 / 全屏)不在本 feat 范围内,由集成方按宿主应用实现;本 feat 只保证桌面布局(§1.1)与 `/both` 共存(§5.1)在现代桌面浏览器下可用,且 inline 审阅控制不被遮挡。

### 5.1 `/both` 路由:inline + chat 共存

共享同一 `aiBusyState`,inline 进行中时 chat 输入框置灰,反之亦然:

```text
┌────────────────────────────┬──────────────────────────────┐
│ TapNote [✨ AI ▾] [☾]      │ 对话助手              [✕]    │
├────────────────────────────┼──────────────────────────────┤
│                            │ 引用: [选区|全文|无]         │
│  BlockNote 编辑器          │                              │
│                            │ ▌用户:加一个总结小标题     │
│  - 第一点...               │   § 全文 12 块               │
│  - 第二点...               │                              │
│  ┌────────────────────┐   │ ◆ AI                        │
│  │ AIMenu 浮层        │   │  已分析全文。               │
│  │ [输入指令...]      │   │  ◔ insertBlock 输入中 #h1│
│  │ [发送] [中止]      │   │                              │
│  └────────────────────┘   │                              │
│  ↑ inline 进行中          │                              │
│    chat 输入框置灰        ├──────────────────────────────┤
│                           │ [输入框置灰]                  │
│                           │ ⏸ 内联 AI 进行中             │
└────────────────────────────┴──────────────────────────────┘
   任一助手完成 / 中止 / 失败后释放 busy,另一助手立即可用
```

## 6. UI 验收标准

- 用户能通过键盘完成打开、选上下文、输入、发送、中止、多轮（§16 item 8 可访问性）。
- 上下文超限、认证/流失败、revision 冲突、工具失败均有可行动提示（§16 item 6/7/10）。
- 每个对话工具调用显示目标与结果；不引用状态不误导为已发送文档（§16 item 6）。
- 选区超 4K 前端拦截提示；引用全文超预算显示截断标记（§16 item 9）。
- 现代桌面浏览器不遮挡编辑内容或接受/拒绝控制。

## 7. 交互参考

| 来源 | 日期 | 借鉴 | 限制 |
|---|---|---|---|
| Cursor Chat / GitHub Copilot Chat | 2026-07-17 | 侧边对话面板、引用选区/文件、离散工具调用、多轮 | 闭源，仅体验对标 |
| AI SDK 官方 UI 文档 | 2026-07-17 | 工具流状态与结果呈现、UIMessage part 渲染 | API 版本待锁定 |

## 8. 待确认事项

- `needsApproval` 审批开关为 P2 候选，当前 UI 不应暗示存在该开关（SUB-003 ui.md §6）。
- AI SDK React/UI 包版本与 `useChat`/client-side tools UI API 须实施前锁定（总 PRD §17 item 5）。

## 9. 设计决策记录

| 决策 | 备选 | 选择理由 | 影响 |
|---|---|---|---|
| 桌面 ChatPanel 为可开合右侧抽屉(380–420px 固定宽) | (A) 常驻固定列 (B) 可开合抽屉 (C) 浮层 overlay | 用户选 B;编辑器宽度自适应,空间利用率高且需要时才占位 | 抽屉关闭时编辑器占满,header 需要切换按钮;焦点关闭后回到编辑器 |
| 工具调用与 AI 文本消息归属:两者结合 | (A) 嵌入同一条 UIMessage.parts (B) 独立工具气泡 (C) 两者结合 | 用户选 C;tool-call 输入状态嵌入 AI 消息(符合 UIMessage.parts 模型),tool 结果(成功/冲突/前置失败)用独立气泡作回执 | 消息列表需渲染两种 part:UIMessage.parts + 工具结果独立气泡;状态联动需同步 |
| 上下文三态选择器:三段式 segmented control | (A) segmented (B) 下拉 (C) 单按钮循环 | 用户选 A;三态可见性高,默认 `无`,切换时编辑器侧保留高亮 | 占用 1 行宽度;超限提示在 segmented 下方条件出现 |
| 「引用选区」编辑器侧保留选区高亮直到发送 | (A) 保留高亮 (B) 仅气泡 chip (C) 两者都做 | 用户选 C;发送前编辑器侧高亮 + 消息气泡 chip,发送后清除编辑器侧高亮保留 chip 作为审计回执 | 需要 editor 选区状态 hook + 跨组件协调高亮 lifecycle |
| 冲突气泡「仅重试该操作」 | (A) 重发整轮 (B) 仅重试该 tool-call (C) 两者结合 | 用户选 B;用最新 revision 重新 execute 当前 `toolCallId`,不破坏已成功操作(tech.md §6),节省 token | 重试逻辑需以 toolCallId 为粒度,失败 N 次后的兜底另行讨论(P2) |
