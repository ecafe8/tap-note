{
  "name": "tap-note",
  "version": "0.0.1",
  "devDependencies": {
    "mdts": "^0.20.3",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "turbo": "^2.9.18",
    "typescript": "~6"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "bun@1.3.14",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "submodule:init": "git submodule update --init --recursive --progress",
    "docs": "mdts ./docs -p 8881"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@fontsource/noto-sans-sc": "^5.3.0"
  }
}
